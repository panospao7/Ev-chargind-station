# Database scripts

Migration pipeline and developer database commands are owned by delivery
task **I1-DAT-001** (persistence foundation). Intentionally empty on the
runway; see `infra/local/postgres/README.md` for the provisioning boundary.
