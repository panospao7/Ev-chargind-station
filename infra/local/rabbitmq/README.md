# RabbitMQ configuration (broker tasks own this)

Topology (vhost, durable exchanges, retry/DLQ definitions) is defined by the
messaging delivery tasks against the approved AsyncAPI contract model.
Intentionally empty on the runway.
