# Keycloak realm imports (identity task owns this)

Realm/client configuration (ev-platform realm, BFF and service clients) is
delivered by the identity/BFF delivery task (POC-01). Intentionally empty on
the runway; `--import-realm` picks up files placed here.
