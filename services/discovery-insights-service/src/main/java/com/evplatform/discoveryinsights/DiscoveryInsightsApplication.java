package com.evplatform.discoveryinsights;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

/**
 * DiscoveryInsights Service entry point.
 *
 * Runway skeleton only: it intentionally contains no business capability.
 * Behavior is delivered by the owning delivery tasks against approved
 * contracts and migrations.
 */
@SpringBootApplication
public class DiscoveryInsightsApplication {

    public static void main(String[] args) {
        SpringApplication.run(DiscoveryInsightsApplication.class, args);
    }
}
